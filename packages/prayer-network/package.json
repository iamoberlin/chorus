{
  "name": "prayer-network",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create prayers",
    "db:init": "wrangler d1 execute prayers --file=./schema.sql",
    "db:init:local": "wrangler d1 execute prayers --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.25.0"
  }
}
